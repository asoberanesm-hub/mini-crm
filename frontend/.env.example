# URL del backend (API)
VITE_API_URL=http://localhost:3001

# Clerk (login). Obtén la clave en https://dashboard.clerk.com → Your application → API Keys
# Publishable Key: se usa en el frontend (puede ser pública)
VITE_CLERK_PUBLISHABLE_KEY=
